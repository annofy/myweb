import React from 'react'
import {
  Button,
  Form,
  Input
} from 'antd'

const FormItem = Form.Item

export default class Register extends React.Component {
  constructor(props) {
    super(props)
  }

  render() {
    return <div>我是注册页面</div>
  }
}