const ArticleModel = require('./ArticleModel')
const CategoryModel = require('./CategoryModel')
const User = require('./User')
const EntityModel = require('./EntityModel')
module.exports = {
  ArticleModel, CategoryModel, User, EntityModel
}